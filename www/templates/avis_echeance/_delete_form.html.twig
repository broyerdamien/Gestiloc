<form method="post" action="{{ path('app_avis_echeance_delete', {'id': avis_echeance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avis_echeance.id) }}">
    <input type="hidden" name="_redirect_to" value="{{ app.request.uri }}">
    <button class="btn btn-danger">Delete</button>
</form>
